<% include ../header %>

<div style="margin: 1px 20px">
    <div class="ui blue  piled attached segment">
        <div class="ui blue header">
            请选择模板尺寸
        </div>
        <div class="ui divider"></div>
        <% templates.forEach( function(template) { %>
        <a href="/ruby/<%= template.id %>" class="ui tag label"><%= template.name %></a>
        <% }) %>
        
    </div>


</div>

<% include ../footer %>